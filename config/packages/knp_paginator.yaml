# knp_paginator:
#   page_range: 5
#   template:
#     pagination: '@KnpPaginator/Pagination/bootstrap_v5_pagination.html.twig'
#   default_options:
#     page_name: page                # page query parameter name
#     sort_field_name: sort          # sort field query parameter name
#     sort_direction_name: direction # sort direction query parameter name
#     distinct: false                # ensure distinct results, useful when ORM queries are using GROUP BY statements
# template:
#     pagination: 'KnpPaginatorBundle:Pagination:twitter_bootstrap_v5_pagination.html.twig'     # sliding pagination controls template
#     sortable: 'KnpPaginatorBundle:Pagination:sortable_link.html.twig' # sort link template
