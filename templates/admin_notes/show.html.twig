{% extends 'base-backend.html.twig' %}

{% block title %}Notes{% endblock %}

{% block body %}
    <h1>Notes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ note.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ note.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_notes_index') }}">back to list</a>

    <a href="{{ path('app_admin_notes_edit', {'id': note.id}) }}">edit</a>

    {{ include('admin_notes/_delete_form.html.twig') }}
{% endblock %}
