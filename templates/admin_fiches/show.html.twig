{% extends 'base-backend.html.twig' %}

{% block title %}Fiches{% endblock %}

{% block body %}
    <h1>Fiches</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fich.id }}</td>
            </tr>
            <tr>
                <th>FileName</th>
                <td>{{ fich.fileName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_fiches_index') }}">back to list</a>

    <a href="{{ path('app_admin_fiches_edit', {'id': fich.id}) }}">edit</a>

    {{ include('admin_fiches/_delete_form.html.twig') }}
{% endblock %}
