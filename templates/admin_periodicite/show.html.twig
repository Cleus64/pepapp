{% extends 'base-backend.html.twig' %}

{% block title %}Periodicite{% endblock %}

{% block body %}
    <h1>Periodicite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ periodicite.id }}</td>
            </tr>
            <tr>
                <th>Groupe</th>
                <td>{{ periodicite.groupe }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_periodicite_index') }}">back to list</a>

    <a href="{{ path('app_admin_periodicite_edit', {'id': periodicite.id}) }}">edit</a>

    {{ include('admin_periodicite/_delete_form.html.twig') }}
{% endblock %}
